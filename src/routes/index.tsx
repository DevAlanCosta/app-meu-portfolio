import BottomTabRoutes from './botom-tabs.routes';
import { NavigationContainer } from '@react-navigation/native';

function Routes() {
  return (
    <NavigationContainer>
      <BottomTabRoutes />
    </NavigationContainer>
  );
}

export default Routes;
